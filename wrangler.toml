name = "orbiting"
type = "javascript"

[build]
command = "npm install && npx expo export --platform web"
cwd = "packages/chat"
upload.format = "service-worker"

[site]
bucket = "./dist"
entry-point = "."

[vars]
NODE_VERSION = "20"
NPM_VERSION = "10"
